# runtime/
> L2 | 父级: ../CLAUDE.md

package.json: 精确声明官方 Harness、pnpm、npm 发布插件；overrides 统一预发布 peer，避免重复 Host 服务。
package-lock.json: 全部运行依赖的版本、来源与完整性锁；由目标平台 npm ci 安装匹配的原生包。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
