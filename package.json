{
  "name": "owndsh-desktop",
  "version": "0.1.7-alpha.1",
  "private": true,
  "type": "module",
  "description": "OwnDsh distribution of the official DeepSeek Harness Electron desktop",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": "24.14.1"
  },
  "scripts": {
    "prepare:runtime": "node build.mjs --prepare-only",
    "build": "node build.mjs",
    "test": "node --test desktop.test.mjs",
    "test:app": "node --test desktop-app.test.mjs",
    "prepare:source": "node build.mjs --source-only"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "playwright": "1.58.2",
    "pnpm": "11.7.0",
    "sharp": "0.35.3"
  }
}
